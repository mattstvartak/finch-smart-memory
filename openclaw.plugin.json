{
  "id": "openclaw-smart-memory-plugin",
  "name": "Smart Memory",
  "description": "Intelligent memory manager with LLM-powered extraction, hybrid ANN vector search, tier lifecycle, spreading activation, procedural rules, WAL capture, Mem0 cloud integration, and session-state hot RAM.",
  "kind": "memory",
  "providerAuthEnvVars": {
    "openclaw-smart-memory-plugin": ["MEM0_API_KEY"]
  },
  "providerAuthChoices": [
    {
      "provider": "openclaw-smart-memory-plugin",
      "method": "api-key",
      "choiceId": "mem0-key",
      "choiceLabel": "Mem0 API Key (optional, only for Mem0 cloud extraction)",
      "cliOption": "--mem0-api-key <key>",
      "groupId": "openclaw-smart-memory-plugin",
      "groupLabel": "OpenClaw Smart Memory",
      "groupHint": "No API key required for local extraction — uses your existing OpenClaw model provider"
    }
  ],
  "contracts": {
    "tools": [
      "memory_search",
      "memory_format",
      "memory_ingest",
      "memory_extract",
      "memory_maintain",
      "memory_rules",
      "memory_outcome",
      "memory_session",
      "memory_stats",
      "memory_mem0_sync"
    ]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "extractionProvider": {
        "type": "string",
        "enum": ["local", "mem0", "both"],
        "default": "local",
        "description": "Extraction provider: local (your OpenClaw model), mem0 (Mem0 cloud), or both."
      },
      "mem0ApiKey": {
        "type": "string",
        "default": "",
        "description": "Mem0 API key for cloud extraction. Falls back to MEM0_API_KEY env var."
      },
      "mem0UserId": {
        "type": "string",
        "default": "default",
        "description": "Mem0 user scope identifier."
      },
      "maxRecallChunks": {
        "type": "number",
        "default": 10,
        "minimum": 1,
        "maximum": 50,
        "description": "Max memories returned per search."
      },
      "maxRecallTokens": {
        "type": "number",
        "default": 1500,
        "minimum": 100,
        "maximum": 10000,
        "description": "Token budget for recalled memories."
      },
      "autoExtract": {
        "type": "boolean",
        "default": true,
        "description": "Automatically extract memories after conversations."
      },
      "autoMaintain": {
        "type": "boolean",
        "default": true,
        "description": "Run consolidation at session start."
      }
    }
  },
  "uiHints": {
    "extractionProvider": {
      "label": "Extraction Provider",
      "help": "Which service extracts memories from conversations."
    },
    "mem0ApiKey": {
      "label": "Mem0 API Key",
      "help": "API key for Mem0 cloud extraction. Leave empty to use MEM0_API_KEY env var instead.",
      "sensitive": true
    },
    "mem0UserId": {
      "label": "Mem0 User ID",
      "help": "User identifier for Mem0 cloud scoping."
    },
    "maxRecallChunks": {
      "label": "Max Recall Results",
      "help": "Maximum number of memories returned per search."
    },
    "maxRecallTokens": {
      "label": "Recall Token Budget",
      "help": "Maximum tokens used by recalled memories in the prompt."
    }
  }
}
